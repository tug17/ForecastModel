from setuptools import setup, find_packages

setup(
    name = 'ForecastModel',
    version = '0.1.0',
    url = 'https://github.com/tug17/ForecastModel',
    description = '',
    packages = find_packages(),
    install_requires = [
    ]
)
